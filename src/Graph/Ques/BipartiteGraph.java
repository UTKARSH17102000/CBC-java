package Graph.Ques;

//Graph which can be colored using two colors such that no two adjacent nodes have same colors
//Any graph which have add length cycle can not be a bipartite graph
//We will use color array with 0&1 and a queue to traverse through the graph
// we will iterate every node and mark its color in color array and add it to the queue  and check if the node and its adjacent node have same color then we will return false'

public class BipartiteGraph {
    public static void main(String[] args) {

    }
}
